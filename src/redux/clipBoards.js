import { createSlice } from '@reduxjs/toolkit'

const initialState = {};

const clipBoardsSlice = createSlice({
    name: 'clipBoardsSlice',
    initialState,
    reducers: {}
});

// export const { } = clipBoardsSlice.actions;

export default clipBoardsSlice.reducer;